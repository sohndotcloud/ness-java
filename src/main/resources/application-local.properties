spring.application.name=ness-backend

# --- Database ---
spring.datasource.url=jdbc:postgresql://ep-flat-unit-ayg3v2gf.c-5.us-east-2.aws.neon.tech/neondb?sslmode=require&connectTimeout=10&socketTimeout=30
spring.datasource.username=neondb_owner
spring.datasource.password=npg_wWGzEQ0dyFh7
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Flyway ---
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# --- JWT ---
#app.jwt.secret=${JWT_SECRET}
app.jwt.secret=Kx7mP2vN9qL4wR8tY3zA6bC1dE5fG0hJ+xU/oI=
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
app.cors.allowed-origin=http://localhost:5173
server.port=${SERVER_PORT:8080}
app.cookie.domain=