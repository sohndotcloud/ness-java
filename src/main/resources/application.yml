spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/nessdb
    username: nessbackend
    password: coronai vanza
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
  jwt:
    secret: ${JWT_SECRET}
    expiration-ms: 86400000